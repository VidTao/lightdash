config-version: 2
name: bratrax
version: 1.0.0
profile: bratrax
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - data
macro-paths:
  - macros
snapshot-paths:
  - snapshots
clean-targets:
  - target
  - dbt_packages
models:
  bratrax:
    example:
      +materialized: view
target-path: target
